# VoidOCR: symmetric observation choices

Both before and after the pause use the same 12-state vocabulary. An unchanged state or a transition such as clarity → tension is a valid self-report. The interface does not require improvement.

Trace schema 1.1.0 and existing saved traces remain compatible. Meta Core accepts the expanded vocabulary and preserves the reported pair. Stability still controls the local ALLOW/DENY gate (ALLOW at 2–3); it does not establish improvement, measured effectiveness, or observed Q. Unknown Q remains null. Existing journal verification, expiry and replay checks remain in force.

Regression coverage: unchanged and reversed observations, zero stability, incomplete/invalid input, handoff round-trip preservation, DENY rejection, unknown-Q HOLD, and versioned offline assets. This is a local self-report interface, not evidence of therapeutic or external effects.
